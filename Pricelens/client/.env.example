# API URL – frontend talks to this backend. Copy to .env and set for your environment.
#
# Development (simulator / same machine):
#   EXPO_PUBLIC_API_URL=http://localhost:3000
#
# Development (physical device on same Wi‑Fi; get IP: ifconfig | grep "inet "):
#   EXPO_PUBLIC_API_URL=http://YOUR_MAC_IP:3000
#
# Production (backend on Render):
#   EXPO_PUBLIC_API_URL=https://YOUR-SERVICE-NAME.onrender.com
#   (Use the exact URL from Render dashboard, no trailing slash.)
#
# After changing .env, restart the Expo dev server and rebuild the app so the new URL is used.

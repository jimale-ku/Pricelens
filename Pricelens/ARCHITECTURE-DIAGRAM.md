# 🏗️ PriceLens Architecture Diagram

## **System Flow**

```
┌─────────────────────────────────────────────────────────────┐
│                    USER INTERACTION                          │
│  (Mobile App - React Native/Expo)                           │
└───────────────────────┬─────────────────────────────────────┘
                        │
                        │ HTTP/REST API Calls
                        │ (JSON Requests & Responses)
                        │
┌───────────────────────▼─────────────────────────────────────┐
│                    BACKEND API SERVER                        │
│                    (NestJS - Port 3000)                      │
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  Controllers │  │   Services   │  │   Database   │     │
│  │  (Routes)    │→ │  (Logic)     │→ │   (Prisma)   │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└───────────────────────┬─────────────────────────────────────┘
                        │
        ┌───────────────┼───────────────┐
        │               │               │
┌───────▼──────┐ ┌──────▼──────┐ ┌─────▼──────┐
│  PostgreSQL  │ │  PriceAPI    │ │   Redis    │
│  Database    │ │  (External)  │ │  (Jobs)    │
│              │ │              │ │            │
│  - Products  │ │  - Real      │ │  - Price   │
│  - Stores    │ │    prices    │ │    updates │
│  - Prices    │ │  - Product   │ │  - Alerts  │
│  - Users     │ │    data      │ │            │
└──────────────┘ └──────────────┘ └────────────┘
```

---

## **Example: Product Search Flow**

```
1. USER TYPES "bananas" in app
   │
   ▼
2. FRONTEND makes API call:
   GET /products/compare/multi-store?q=bananas
   │
   ▼
3. BACKEND receives request
   │
   ├─→ Check database for "bananas"
   │   │
   │   ├─→ Found? Return from database (FAST)
   │   │
   │   └─→ Not found? Call PriceAPI
   │       │
   │       └─→ Get prices from Walmart, Amazon, Target
   │           │
   │           └─→ Save to database for next time
   │
   ▼
4. BACKEND returns JSON:
   {
     product: { name: "Bananas", image: "..." },
     prices: [
       { store: "Costco", price: 0.49 },
       { store: "Walmart", price: 0.53 }
     ]
   }
   │
   ▼
5. FRONTEND displays:
   - Product card with image
   - List of prices from all stores
   - "Best Price" badge on cheapest
```

---

## **Key Endpoints**

```
┌─────────────────────────────────────────────────────────┐
│  PRODUCT SEARCH                                          │
│  GET /products/compare/multi-store?q={query}             │
│  → Returns product with prices from all stores           │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  POPULAR PRODUCTS                                        │
│  GET /products/popular?categorySlug=groceries            │
│  → Returns popular products for category                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  STORES                                                  │
│  GET /stores                                             │
│  → Returns all enabled stores with logos                 │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│  CATEGORIES                                              │
│  GET /categories                                         │
│  → Returns all product categories                        │
└─────────────────────────────────────────────────────────┘
```

---

## **Data Flow**

```
USER ACTION
    │
    ▼
FRONTEND COMPONENT
    │
    ▼
API SERVICE (categoryService.ts)
    │
    ▼
HTTP REQUEST
    │
    ▼
BACKEND CONTROLLER (products.controller.ts)
    │
    ▼
BACKEND SERVICE (products.service.ts)
    │
    ├─→ Database Query (Prisma)
    │   └─→ PostgreSQL
    │
    └─→ External API (PriceAPI)
        └─→ PriceAPI Service
    │
    ▼
RESPONSE (JSON)
    │
    ▼
DATA TRANSFORMATION (apiTransform.ts)
    │
    ▼
FRONTEND DISPLAY
    │
    ▼
USER SEES RESULTS
```

---

## **Technology Stack**

```
┌─────────────────────────────────────────┐
│           FRONTEND                      │
├─────────────────────────────────────────┤
│ • React Native (Expo)                   │
│ • TypeScript                             │
│ • Tailwind CSS                           │
│ • HTTP Client (fetch API)                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│           BACKEND                        │
├─────────────────────────────────────────┤
│ • NestJS Framework                       │
│ • TypeScript                             │
│ • Prisma ORM                             │
│ • RESTful API                            │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│           DATABASE                       │
├─────────────────────────────────────────┤
│ • PostgreSQL                             │
│ • 15 Tables                              │
│ • Indexed for performance                │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│           EXTERNAL                       │
├─────────────────────────────────────────┤
│ • PriceAPI (price data)                 │
│ • Redis (background jobs)                │
└─────────────────────────────────────────┘
```

---

## **Connection Details**

```
Frontend → Backend:
• Base URL: http://192.168.201.100:3000
• Protocol: HTTP/REST
• Format: JSON
• Authentication: JWT tokens (when logged in)

Backend → Database:
• Type: PostgreSQL
• ORM: Prisma
• Connection: Connection pool
• Queries: Optimized with indexes

Backend → PriceAPI:
• Type: REST API
• Authentication: API Key
• Rate Limiting: Yes
• Caching: Yes (saves to database)
```

---

## **Status Indicators**

```
✅ = Working
⚠️ = In Progress
❌ = Not Started

Frontend-Backend Connection: ✅
Product Search: ✅
Category Pages: ✅
Store Management: ✅
Price Comparison: ✅
Authentication: ✅
Shopping Lists: ⚠️
Price Alerts: ⚠️
Background Jobs: ⚠️
```

---

*Use this diagram to explain the architecture during your meeting!*














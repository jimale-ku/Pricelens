// Legacy Prisma 7 config file.
// With Prisma 5 we don't need a custom config – the CLI reads `prisma/schema.prisma`
// directly and picks up `env("DATABASE_URL")` from there.
// This file is left as a no-op to keep TypeScript and tooling happy.

export {};

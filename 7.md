7. MOBILE (React Native) TRANSLATION STRATEGY
Recommended Architecture Changes
WEB VERSION                          MOBILE VERSION
─────────────────────────────────   ─────────────────────────────────
Single Page App                  →   Stack Navigation App
│                                    │
├── App.tsx (all tabs)           →   ├── Navigation/
│   ├── Header (sticky)          →   │   ├── AuthStack
│   ├── Tabs (42 pills)          →   │   │   └── LoginScreen
│   └── TabsContent (42)         →   │   │
│                                    │   └── MainStack
│                                    │       ├── HomeScreen (dashboard)
│                                    │       ├── CategoryStack
│                                    │       │   ├── GroceriesScreen
│                                    │       │   ├── ElectronicsScreen
│                                    │       │   └── ... (39 more)
│                                    │       ├── ListScreen
│                                    │       ├── PlusScreen
│                                    │       └── ProfileScreen
File Structure for Mobile
/src
  /navigation
    AppNavigator.tsx           # Root navigator
    AuthStack.tsx             # Login flow
    MainStack.tsx             # Authenticated screens
    
  /screens
    /auth
      LoginScreen.tsx
      SignUpScreen.tsx
      
    HomeScreen.tsx            # Category grid (replaces tabs)
    
    /categories               # 39 category screens
      GroceriesScreen.tsx
      ElectronicsScreen.tsx
      ...
      
    /features
      GroceryListScreen.tsx
      PriceLensPlusScreen.tsx
      ProfileScreen.tsx
      
  /components
    /ui                       # Reusable components (buttons, cards)
    /shared                   # Shared patterns
      CategoryCard.tsx
      PriceComparisonCard.tsx
      ServiceListingCard.tsx
      
  /data
    products.ts               # Same as web
    mockData.ts              # Same as web
    
  /utils
    helpers.ts               # Same utility functions
    
  /styles
    theme.ts                 # Colors, gradients, spacing
HomeScreen Design (Mobile Dashboard)
// /screens/HomeScreen.tsx

import { View, ScrollView, TouchableOpacity, Text } from 'react-native';
import { useNavigation } from '@react-navigation/native';

export function HomeScreen() {
  const navigation = useNavigation();
  
  const categories = [
    { id: 'groceries', name: 'Groceries', icon: '🛒', gradient: ['#3b82f6', '#8b5cf6'] },
    { id: 'electronics', name: 'Electronics', icon: '💻', gradient: ['#8b5cf6', '#ec4899'] },
    // ... 37 more
  ];
  
  return (
    <View style={styles.container}>
      {/* Header */}
      <View style={styles.header}>
        <View style={styles.logoContainer}>
          {/* Eye icon with gradient */}
        </View>
        <Text style={styles.title}>PriceLens</Text>
        <Text style={styles.tagline}>See the Savings Clearly</Text>
      </View>
      
      {/* Search Bar */}
      <TouchableOpacity 
        onPress={() => navigation.navigate('UniversalSearch')}
        style={styles.searchBar}
      >
        <Text style={styles.searchPlaceholder}>Search products, services...</Text>
      </TouchableOpacity>
      
      {/* Category Grid */}
      <ScrollView style={styles.scrollView}>
        <View style={styles.grid}>
          {categories.map(category => (
            <TouchableOpacity
              key={category.id}
              onPress={() => navigation.navigate(category.id)}
              style={styles.categoryCard}
            >
              <LinearGradient
                colors={category.gradient}
                style={styles.iconContainer}
              >
                <Text style={styles.icon}>{category.icon}</Text>
              </LinearGradient>
              <Text style={styles.categoryName}>{category.name}</Text>
            </TouchableOpacity>
          ))}
        </View>
      </ScrollView>
      
      {/* Bottom Tab Bar (My List, Plus, Profile) */}
      <View style={styles.bottomTabs}>
        <TouchableOpacity onPress={() => navigation.navigate('GroceryList')}>
          <Text>📝 My List</Text>
        </TouchableOpacity>
        <TouchableOpacity onPress={() => navigation.navigate('Plus')}>
          <Text>👑 Plus</Text>
        </TouchableOpacity>
        <TouchableOpacity onPress={() => navigation.navigate('Profile')}>
          <Text>⚙️ Profile</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
}
Navigation Implementation
// /navigation/AppNavigator.tsx

import { NavigationContainer } from '@react-navigation/native';
import { createNativeStackNavigator } from '@react-navigation/native-stack';
import { createBottomTabNavigator } from '@react-navigation/bottom-tabs';

const Stack = createNativeStackNavigator();
const Tab = createBottomTabNavigator();

export function AppNavigator() {
  const [isAuthenticated, setIsAuthenticated] = useState(false);
  
  return (
    <NavigationContainer>
      <Stack.Navigator screenOptions={{ headerShown: false }}>
        {!isAuthenticated ? (
          // Auth Stack
          <Stack.Screen name="Login" component={LoginScreen} />
        ) : (
          // Main Stack
          <>
            <Stack.Screen name="Home" component={HomeScreen} />
            
            {/* Category Screens */}
            <Stack.Screen name="Groceries" component={GroceriesScreen} />
            <Stack.Screen name="Electronics" component={ElectronicsScreen} />
            {/* ... 37 more category screens */}
            
            {/* Feature Screens */}
            <Stack.Screen name="GroceryList" component={GroceryListScreen} />
            <Stack.Screen name="Plus" component={PriceLensPlusScreen} />
            <Stack.Screen name="Profile" component={ProfileScreen} />
          </>
        )}
      </Stack.Navigator>
    </NavigationContainer>
  );
}
Component Translation Examples
Web Card → Mobile Card:
// WEB (React)
<div className="glass-card p-6 rounded-xl border border-white/10">
  <h3 className="text-white text-lg">{product.name}</h3>
  <p className="text-slate-400">${product.price}</p>
</div>

// MOBILE (React Native)
<View style={styles.card}>
  <Text style={styles.productName}>{product.name}</Text>
  <Text style={styles.price}>${product.price}</Text>
</View>

const styles = StyleSheet.create({
  card: {
    backgroundColor: 'rgba(15, 23, 42, 0.6)',
    padding: 24,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: 'rgba(255, 255, 255, 0.1)',
  },
  productName: {
    color: '#ffffff',
    fontSize: 18,
  },
  price: {
    color: '#94a3b8',
  },
});
Performance Optimizations for Mobile
1. Lazy Loading: Don't mount all 39 categories
2. // Only load when user navigates
3. <Stack.Screen 
4.   name="Electronics" 
5.   component={ElectronicsScreen}
6.   options={{ lazy: true }}
7. />
8. Image Optimization:
9. // Use React Native Fast Image
10. import FastImage from 'react-native-fast-image';
11. 
12. <FastImage
13.   source={{ uri: product.image, priority: FastImage.priority.normal }}
14.   resizeMode={FastImage.resizeMode.cover}
15. />
16. FlatList for Long Lists:
17. // Instead of .map()
18. <FlatList
19.   data={products}
20.   renderItem={({ item }) => <ProductCard product={item} />}
21.   keyExtractor={item => item.id}
22.   initialNumToRender={10}
23.   maxToRenderPerBatch={10}
24.   windowSize={5}
25. />
26. AsyncStorage (instead of localStorage):
27. import AsyncStorage from '@react-native-async-storage/async-storage';
28. 
29. // Save
30. await AsyncStorage.setItem('pricelens-user', JSON.stringify(user));
31. 
32. // Load
33. const userData = await AsyncStorage.getItem('pricelens-user');
34. const user = JSON.parse(userData);
